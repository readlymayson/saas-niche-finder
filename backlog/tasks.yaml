tasks:
- id: pipeline-vc-ingest-celery
  title: VC ingest + обогащение niche_pipeline
  description: |
    vc_ingest (фикстуры/RSS), niche_pipeline с niche_draft и эмбеддингами,
    Celery ingest_vc_rss, pgvector /similar, Redis-квота.
  acceptance: |
    - pytest и ruff зелёные
  priority: 40
  status: done
  tags: [backend, celery, pipeline]
- id: frontend-search-similar
  title: Frontend — поиск и похожие ниши
  description: |
    api.ts search/similar + 429; App.tsx кнопки поиска и похожих.
  acceptance: |
    - pytest и ruff зелёные
  priority: 25
  status: done
  tags: [frontend, api]
- id: test-auth-register-validation
  title: Тест валидации register (422 без БД)
  description: |
    Добавь tests/test_auth_validation.py: подключи только router auth,
    POST /auth/register с паролем короче 8 символов — ожидай 422.
    Без реальной БД и без вызовов внешних API (только Pydantic/FastAPI validation).
  acceptance: |
    - pytest из корня — exit 0
    - ruff check backend tests — exit 0
  priority: 28
  status: done
  tags: [backend, api, tests, auth]
- id: test-scoring-invalid-string
  title: Тест scoring для невалидной строки
  description: |
    В tests/test_scoring.py добавь один кейс: score_from_payloads с полем growth "not-a-number"
    даёт предсказуемый результат (как для пустой/невалидной строки в _to_float).
    Не меняй формулу calculate_score.
  acceptance: |
    - pytest и ruff зелёные
  priority: 22
  status: done
  tags: [backend, tests]
- id: readme-frontend-dev
  title: README — локальный запуск frontend
  description: |
    Дополни корневой README кратким разделом про frontend/: npm ci, npm run dev,
    URL http://localhost:5173 и связь с API (CORS уже в main.py). Без секретов.
    Можно сослаться на frontend/README.md если там уже есть детали.
  acceptance: |
    - pytest и ruff зелёные
    - в README есть подраздел про frontend
  priority: 16
  status: done
  tags: [docs, frontend]
- id: test-niches-health
  title: Тест health/readiness niches API
  description: 'Добавь минимальный тест для публичного endpoint в backend/app/api/niches.py

    (или существующего health), без внешних API и секретов.

    '
  acceptance: '- pytest из корня репозитория завершается с кодом 0

    - ruff check backend tests без ошибок

    '
  priority: 30
  status: done
  tags:
  - backend
  - api
  - tests
- id: ruff-services-cleanup
  title: Один autofix ruff в services
  description: 'Исправь одно предупреждение ruff в backend/app/services/ (импорты,
    unused, простой стиль).

    Не меняй поведение API.

    '
  acceptance: '- ruff check backend tests — exit 0

    - pytest — exit 0

    '
  priority: 20
  status: done
  tags:
  - backend
  - ruff
- id: readme-env-doc
  title: Документировать локальный запуск в README
  description: 'Добавь в README (или backend/README) раздел: venv, pip install -e
    "./backend[dev]",

    pytest и ruff из корня — без реальных секретов, только .env.example.

    '
  acceptance: '- pytest и ruff зелёные

    - в diff есть новый/обновлённый раздел документации

    '
  priority: 10
  status: done
  tags:
  - docs
- id: schema-niches-typing
  title: Уточнить типы в schemas/niches
  description: 'Улучши аннотации или docstring в backend/app/schemas/niches.py для
    одной модели ответа.

    Минимальный diff.

    '
  acceptance: '- pytest и ruff зелёные

    '
  priority: 15
  status: done
  tags:
  - backend
  - api
